# Gemini API key for the "Interview My AI Clone" chat (used by server-side /api/chat).
# Get a key at: https://aistudio.google.com/app/apikey
# On Vercel: add as GEMINI_API_KEY in Project → Settings → Environment Variables, then redeploy.
GEMINI_API_KEY=
